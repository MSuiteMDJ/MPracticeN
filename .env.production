# Production Environment - Desktop App (local-first)
# Empty API URL = same-origin requests; the bundled local backend serves the UI.
VITE_API_URL=
VITE_APP_NAME=Arcanus Practice
VITE_APP_VERSION=1.0.0
VITE_ENV=production
